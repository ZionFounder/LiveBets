<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>FlipNugz Dice Bet</title>
  <style>
    body {
      background-color: #111;
      color: #fff;
      font-family: Arial, sans-serif;
      text-align: center;
      padding: 30px;
    }
    h1 {
      color: #ffd700;
    }
    .numbers {
      display: flex;
      justify-content: center;
      gap: 20px;
      flex-wrap: wrap;
      margin-top: 20px;
    }
    .number-button {
      background-color: #222;
      color: #fff;
      border: 2px solid #ffd700;
      border-radius: 8px;
      padding: 15px 25px;
      font-size: 18px;
      cursor: pointer;
    }
    .number-button:disabled {
      background-color: #555;
      cursor: not-allowed;
    }
    #confirmation {
      margin-top: 30px;
      font-size: 16px;
      color: #0f0;
    }
  </style>
</head>
<body>
  <h1>🎲 FlipNugz Dice Betting</h1>
  <p>Select a number and enter your wallet address to reserve it. Send 1 SOL to: <b>YOUR_SOL_WALLET_ADDRESS</b></p>

  <div class="numbers" id="numberGrid"></div>

  <div style="margin-top: 20px;">
    <input type="text" id="walletInput" placeholder="Enter your Solana wallet" style="padding: 10px; width: 300px;" />
  </div>

  <div id="confirmation"></div>

  <script>
    const numberGrid = document.getElementById('numberGrid');
    const confirmation = document.getElementById('confirmation');
    const walletInput = document.getElementById('walletInput');

    const takenNumbers = [];

    for (let i = 1; i <= 6; i++) {
      const btn = document.createElement('button');
      btn.textContent = i;
      btn.className = 'number-button';
      btn.onclick = () => {
        const wallet = walletInput.value.trim();
        if (!wallet) {
          alert("Please enter your wallet address first.");
          return;
        }
        if (takenNumbers.includes(i)) {
          alert("This number is already taken.");
          return;
        }
        takenNumbers.push(i);
        btn.disabled = true;
        confirmation.innerHTML = `✅ <b>Number ${i}</b> reserved for wallet: <b>${wallet}</b><br>Send 1 SOL to: <b>YOUR_SOL_WALLET_ADDRESS</b>`;
      };
      numberGrid.appendChild(btn);
    }
  </script>
</body>
</html>

